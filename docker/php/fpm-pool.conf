; Pool tuning for concurrent requests (dev/staging).
; UID 1000 matches the typical bind-mount owner on Linux dev machines.
[www]
user = 1000
group = 1000
pm = dynamic
pm.max_children = 20
pm.start_servers = 4
pm.min_spare_servers = 2
pm.max_spare_servers = 6
pm.max_requests = 500
